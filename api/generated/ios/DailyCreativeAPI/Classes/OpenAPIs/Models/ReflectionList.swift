//
// ReflectionList.swift
//
// Generated by openapi-generator
// https://openapi-generator.tech
//

import Foundation
#if canImport(AnyCodable)
import AnyCodable
#endif

/** Cursor-paginated Reflection thread page (oldest to newest). */
public struct ReflectionList: Codable, JSONEncodable, Hashable {

    public var items: [Reflection]
    /** Opaque cursor for the next page, or null when no further pages exist. */
    public var nextCursor: String?

    public init(items: [Reflection], nextCursor: String?) {
        self.items = items
        self.nextCursor = nextCursor
    }

    public enum CodingKeys: String, CodingKey, CaseIterable {
        case items
        case nextCursor = "next_cursor"
    }

    // Encodable protocol methods

    public func encode(to encoder: Encoder) throws {
        var container = encoder.container(keyedBy: CodingKeys.self)
        try container.encode(items, forKey: .items)
        try container.encode(nextCursor, forKey: .nextCursor)
    }
}

